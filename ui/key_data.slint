export struct KeyData {
    name: string,
    display_name: string,
    is_pressed: bool,
    x: length,
    y: length,
    w: length,
    h: length,
    pressed_color: color,
    color_hex: string,
    selected: bool,
}